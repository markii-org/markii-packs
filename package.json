{
  "name": "markii-packs",
  "private": true,
  "description": "Curated component packs for Markii.",
  "type": "module",
  "scripts": {
    "check": "node scripts/validate.mjs && tsc --noEmit && node scripts/build-check.mjs && node scripts/render-check.mjs"
  },
  "devDependencies": {
    "typescript": "^5",
    "esbuild": "^0.28.2",
    "@markii/pack": "^0.10.0",
    "@markii/react": "^0.10.0",
    "@types/react": "^18",
    "react": "^18",
    "react-dom": "^18"
  }
}
